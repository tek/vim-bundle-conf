" install newly added bundles
nnoremap <leader>Bn :silent! w<Bar>so ~/.vim/vim.vim<Bar>NeoBundleInstall<cr>

" update all bundles
nnoremap <leader>Bu :silent! w<Bar>so ~/.vim/vim.vim<Bar>NeoBundleInstall!<cr>
